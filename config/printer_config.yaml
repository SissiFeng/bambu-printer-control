# Printer Configuration
printer:
  ip: "192.168.1.124"
  serial: "0309CA471800852"
  access_code: "14011913"

# Print Settings
print:
  default_nozzle_temp: 220
  default_bed_temp: 60
  default_print_speed: 60
  
# Grid Settings
grid:
  size: [10, 10]      # 10x10 grid
  square_size: 10     # 10mm square
  gap: 5             # 5mm gap
  start_pos: [32.4, 145]  # start position

# Database Settings
database:
  type: "sqlite"
  path: "print_history.db"
  backup_dir: "backups"
  backup_interval: 86400  # 24 hours in seconds
  keep_backups_days: 30   # Keep backups for 30 days

# MQTT Settings
mqtt:
  host: "mqtt.bambulab.com"
  port: 8883
  username: "bblp"
  password: "bblp"
  printer_serial: "0309CA471800852"
